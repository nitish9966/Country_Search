import { ErrorProps } from '@kinde-oss/kinde-auth-pkce-js';
import React from 'react';
import { KindeUser } from './types';
type KindeProviderProps = {
    audience?: string;
    children: React.ReactNode;
    clientId?: string;
    domain: string;
    isDangerouslyUseLocalStorage?: boolean;
    logoutUri?: string;
    redirectUri: string;
    onRedirectCallback?: (user: KindeUser, state?: Record<string, unknown>) => void;
    onErrorCallback?: (props: ErrorProps) => void;
    scope?: string;
};
export declare const KindeProvider: ({ audience, scope, clientId, children, domain, isDangerouslyUseLocalStorage, redirectUri, onRedirectCallback, onErrorCallback, logoutUri }: KindeProviderProps) => JSX.Element;
export {};
//# sourceMappingURL=KindeProvider.d.ts.map